export const shower = () => {
    // let time = 4000;
    let targetModal = document.getElementById('preOrderNotification')

    targetModal.showModal()

    // setTimeout(() => {
    //     targetModal.close()
    // },time)
}
export const closer = () => {
    let targetModal = document.getElementById('preOrderNotification')

    targetModal.close()

    // setTimeout(() => {
    //     targetModal.close()
    // },time)
}
// export default  shower